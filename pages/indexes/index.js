/*
/* const updateIndex = async () => {

    if (ceramic.did !== undefined) {
      const updateindex = await compose.executeQuery(`
        mutation {
          updateIndex(input: {
            id: "${streamIDs}"
            content: {
              title: "${titleInput}"
            }
          }) 
          {
            document {
              id
              title
            }
          }
        }
      `);
    }
  };
*/
